

const ShowPaymentHistroy = ({history}) => {

   console.log();

    
    return (
        <div>
            {
                // hs.map((h, i)=> <div key={i}></div> )
            }
            
        </div>
    );
};

export default ShowPaymentHistroy;